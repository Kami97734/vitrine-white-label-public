services:
  vitrine:
    build:
      context: .
      dockerfile: Dockerfile
    image: vitrine-virtual-white-label:latest
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
